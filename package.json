{
  "name": "react-elm-app",
  "version": "0.1.0",
  "description": "Add Elm to React for the logic and effects",
  "main": "index.js",
  "scripts": {
    "build": "export NODE_ENV=development && ./node_modules/.bin/babel --plugins transform-object-rest-spread src/main.js --out-file index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "elm",
    "app",
    "react",
    "redux",
    "state"
  ],
  "author": "Pietro Grandi",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-react-app": "^2.2.0"
  },
  "babel": {
    "presets": [
      "react-app"
    ]
  },
  "dependencies": {
    "react": "^15.5.4"
  }
}
